---
import type { CollectionEntry } from 'astro:content';
import PostCard from '../ui/card/PostCard.astro';

interface Props {
  posts: CollectionEntry<'blog'>[];
  title?: string;
}

const { posts = [], title = 'The Chronicles' } = Astro.props as Props;
---

<section class="space-y-6">
  <div class="blog-grid">
    {posts.map((post) => (
      <PostCard post={post} />
    ))}
  </div>
</section>


